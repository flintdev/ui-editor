# ui-editor

```jsx
<UIEditor
    editorData={editorData}
    onSave=(editorData => {})
    stateData={stateData}
    stateOnUpdate={(type, data) => {}}
    actions={actions}
    actionOnUpdate={(type, data) => {}}
/>
```