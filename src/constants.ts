
export enum ActionOperationType {
    Add = 'Add',
    Update = 'Update',
    Delete = 'Delete'
}

export enum StateUpdaterOperationType {
    Add = 'Add',
    Update = 'Update',
    Delete = 'Delete'
}

export enum LOADING_STATUS {
    NOT_STARTED,
    LOADING,
    COMPLETE,
    FAILED
}